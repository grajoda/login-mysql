// Configurações da conexão que vão ser usadas no CRUD 

const user = '';
const password = '';
const database = '';

const dbConfig = {
    host: 'localhost',
    port: '3306',
    user: user, 
    password: password,
    database: database
}


module.exports = dbConfig; 